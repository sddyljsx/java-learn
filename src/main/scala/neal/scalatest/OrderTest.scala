package neal.scalatest


object OrderTest {

//  def maxListImpParm[Int](elements: List[Int])(implicit orderer: Int => Ordered[Int]): Int =
//    elements match {
//      case List() => throw new IllegalArgumentException("empty list!")
//      case List(x) => x.asInstanceOf[Int]
//      case List(head, _*) =>
//        val maxRest = maxListImpParm(tail)(orderer)
//        if (orderer(head) > maxRest) head.asInstanceOf[Int]
//        else maxRest
//    }

  def main(args: Array[String]): Unit = {

  }

}
